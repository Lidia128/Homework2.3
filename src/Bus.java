public class Bus extends Transport{

    public Bus (String brend, String model, int productionYear, String productionCountry, String color,
                     int maxSpeed){
        super( brend, model,productionYear,productionCountry,color,maxSpeed );
    }
}
